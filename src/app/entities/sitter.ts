export class Sitter {
  private name: string;
  private gender: string; // Female, Male, Other...
  private birthDate: Date; // We learn about js dates...
  private noCriminalRecord: boolean; // true = no crimes
  private noChildRecord: boolean; // true = not a child criminal
  private hourlyWage: number;
  private address: string;
  private zipCode: string;
  private city: string;

  private calendar: any; // we don't know the type yet...
}